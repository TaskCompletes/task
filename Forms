 $documentrequired = []; 
    foreach ($definitions->getBundles() as $bundle) {
      if ($bundle->getType() === $type) {
        foreach ($bundle->getDocument() as $docu) {
          if ($docu->getType() === 'mandatory' || $docu->getType() === 'optional') {
            foreach ($definitions->getDocuments() as $document) {
              if ($document->getType() === $docu->getRef()) {
                $documentrequired[$docu->getRef()] = [['ref'] => $docu->getRef(), ['type'] => $docu->getType(), ['displayname'] => $document->getDisplayName()];
              }
            }
          }
        }
      }
    }
    var_dump($documentrequired);
    return $documentrequired;

this is causing below error TypeError: Illegal offset type in Drupal\tna_legislation_publishing_submission\Service\AvailableBundles->getAvailableFile() (line 249 of /var/www/web/modules/custom/tna_legislation_publishing_submission/src/Service/AvailableBundles.php).
