{{ title_prefix }}
{% if title %}
  <h1{{ title_attributes }}>{{ title }}
    {% if subtitle %}
      <span class="subtitle">{{ subtitle }}</span>
    {% endif %}
  </h1>
{% endif %}
{{ title_suffix }} can you modify the twig template to display both title and subtitle in same line with space between them
