to ensure that buttons are being added in a consistent method to our forms. We shouldn’t be setting HTML in them, instead this should be a button render element and the twig templates apply the HTML as necessary.
