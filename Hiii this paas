<?php

function example_cron(){
    \Drupal::messenger()->addMessage("hii thi sis from cron");
}

function example_form_alter(&$form, \Drupal\Core\Form\FormStateInterface $form_state, $form_id) {

          if($form_id === "reset_form_password_page"){
            /**\Drupal::messenger()->addMessage("form find successfully");
            if(isset($form["passwordconfirm"])){
              \Drupal::messenger()->addMessage("inside password confirm");
                    $form["passwordconfirm"]["#process"]=array(
                    
                    "user_form_process_password_confirm",
                    "example_form_process_password_confirm");}*/
            //$element_info = element_info('passwordconfirm');
  //$process = $element_info['#process'];
  // Add our process function to the array:
  //$process[] = 'example_process_password_confirm';
  //$form['passwordconfirm']['#process'] = $process;
              
          }

}
function example_form_process_password_confirm($element) {
  \Drupal::messenger()->addMessage(" inside password confirm");
  $element['passwordconfirm']['#title'] = "password match or  not";
  return $element;
}
