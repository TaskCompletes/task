{# File: box-model.html.twig #}

{# Check if the message is not empty #}
{% if message %}
  <div class="box-model">
    <p>{{ message }}</p>
    {# Check if the close button should be shown #}
    {% if showCloseButton %}
      <button class="close-button">Close</button>
    {% endif %}
  </div>
{% endif %}
